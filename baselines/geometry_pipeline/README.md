# Geometry Pipeline Baseline

This baseline represents the current behavior with no algorithmic changes.

The purpose is to measure:

- how noisy the raw geometric obstacle pipeline is
- how much obstacle count jitter exists
- how much downstream avoid logic is affected

Use this as the reference point for every later figure.
