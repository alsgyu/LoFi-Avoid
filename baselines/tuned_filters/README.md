# Tuned Filters Baseline

This stage applies hand-crafted tuning before introducing ML.

Candidate improvements:

- ROI reduction
- max-depth tuning
- plane removal
- temporal persistence rules
- clustering
- shape filters

The key question is how much performance improves before any learned model is added.
