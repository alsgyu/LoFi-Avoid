# Sample Runs

Place exported run directories here if you want a repository-local benchmark set for quick testing.

Expected shape:

```text
examples/sample_runs/
  baseline_empty_field/
    manifest.txt
    depth_metrics.csv
    striker_metrics.csv
```
